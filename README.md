# Dotfiles

These are my dotfiles neovim, tmux, zsh, etc. on Linux.

## Installation

First, clone the repository.

Then, run the bootstrap script to install [nix](https://nixos.org/).

```
cd dotfiles
./bootstrap.sh
```
